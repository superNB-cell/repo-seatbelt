# Demo Assets

Record these demo assets when ready:

- `doctor-demo.gif`: Run `seatbelt doctor` in `examples/demo-project`.
- `snapshot-demo.gif`: Run `seatbelt snapshot`.
- `rules-demo.gif`: Run `seatbelt rules --agents --codex --claude --cursor`.
- `report-demo.gif`: Run `seatbelt report` and show `.repo-seatbelt/report.md`.

Do not include real secrets in screenshots or GIFs.
