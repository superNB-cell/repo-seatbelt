# Social Posts

## X / Twitter

I am building RepoSeatbelt: a small local CLI for running a safety check,
snapshot, and AI-agent rule pack before letting coding agents edit a project.
Windows-first, no telemetry, no network calls.

## Reddit

I made a small open-source CLI called RepoSeatbelt. It checks risky project
paths, looks for likely secrets, creates local snapshots, and generates AI
coding-agent rules before tools like Codex, Claude Code, or Cursor start
editing. It is not a sandbox, just a lightweight pre-agent checklist.

## Hacker News

Show HN: RepoSeatbelt, a local-first CLI for pre-agent safety checks, snapshots,
and rule files before using AI coding agents. Windows-first, Python standard
library core, no telemetry.

## LinkedIn

RepoSeatbelt is a small local-first developer tool for teams and individuals
using AI coding agents. It helps run a pre-agent safety routine: path checks,
likely secret detection, local snapshots, restore into a new folder, and rule
generation for agent workflows.
